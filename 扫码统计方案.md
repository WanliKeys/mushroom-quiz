# 活动扫码访问人数统计方案

## 目标

活动结束后，统计有多少人通过二维码打开了活动页面。

当前活动页地址：

```text
https://wanlikeys.github.io/mushroom-quiz/
```

统计口径选择：**UV（访客数）**

UV 更接近“有多少不同用户/设备扫了二维码并打开页面”。

## 统计方式

使用 **51.LA** 作为统计平台。

原因：

- 适合国内网络环境和微信内置浏览器
- 接入简单
- 后台可以查看 UV、PV、访问时间、来源等数据
- 不影响现有静态页面部署

## 统计口径说明

51.LA 能统计的是：

- 用户扫码后成功打开页面
- 页面加载统计脚本成功
- 统计平台记录到一次访问

不能统计的是：

- 用户只扫码但没有打开页面
- 用户网络异常导致页面没打开
- 浏览器或插件拦截统计脚本

因此，活动结束后应以 51.LA 后台的 **UV** 作为“扫码打开人数”的参考值。

## 接入步骤

### 1. 创建 51.LA 站点

在 51.LA 后台创建一个新站点。

站点地址填写：

```text
https://wanlikeys.github.io/mushroom-quiz/
```

### 2. 获取统计代码

创建站点后，51.LA 会生成一段统计代码，格式通常类似：

```html
<script charset="UTF-8" id="LA_COLLECT" src="..."></script>
```

具体代码以 51.LA 后台提供为准。

### 3. 接入页面

将 51.LA 提供的完整统计代码加入 `index.html` 的 `</body>` 前。

统计脚本应异步加载，不影响：

- 页面图片加载
- 答题逻辑
- 答对/答错音效
- 随机编码显示
- WebP 图片优化

### 4. 部署更新

接入统计代码后，提交并推送到 GitHub Pages。

线上页面仍然使用原地址：

```text
https://wanlikeys.github.io/mushroom-quiz/
```

二维码不需要重新生成。

## 验证方式

### 本地验证

确认页面功能正常：

- 首页正常显示
- 答题逻辑正常
- 答对页正常显示
- 答错页正常显示
- 随机编码正常显示
- 控制台无 JavaScript 报错

### 线上验证

推送上线后：

1. 用微信扫码打开活动页
2. 等待 51.LA 后台数据刷新
3. 检查 UV / PV 是否增加

## 活动结束后查看数据

在 51.LA 后台选择活动日期范围，重点查看：

- UV：访客数，可作为“扫码打开人数”
- PV：页面打开次数，可作为“总访问次数”
- 访问趋势：判断高峰时段
- 设备/浏览器信息：辅助判断用户访问环境

## 注意事项

- 同一个用户换设备或换浏览器，可能会被统计为多个 UV。
- 同一个用户多次打开页面，通常 PV 会增加，UV 不一定增加。
- 如果用户网络环境无法加载 51.LA 脚本，该访问可能不会被统计。
- GitHub Pages 仍可能在微信内访问较慢，但不影响统计方案本身。

## 最终方案

使用 51.LA 接入 `index.html`，活动结束后以后台 UV 作为扫码打开人数统计依据，PV 作为访问次数辅助参考。
